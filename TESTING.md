# iPhone 가로 모드 테스트 가이드

## 테스트 환경
- **iPhone 14 Pro 가로**: 844 × 390px
- **iPhone SE 가로**: 667 × 375px  
- **브라우저**: Safari (모바일)
- **개발자 도구**: Chrome DevTools (Device Mode)

## 수정된 문제들

### 1. 악보 마디 자동 이동 (Critical)
**이전 동작**: 마디가 2개씩 묶여서 표시되어, 음표 9/24에서도 첫 두 마디가 겹쳐 보임
**현재 동작**: 현재 음표가 속한 마디를 기준으로 슬라이딩 윈도우 적용
- **좁은 화면** (width < 900px): 한 번에 1개 마디만 표시
- **넓은 화면** (width >= 900px): 현재 마디 + 다음 마디 (2개) 표시
- 마디 끝 음표 완료 시 즉시 다음 마디로 전환

### 2. 음표 겹침 방지
**문제**: 좁은 화면에서 2개 마디를 한 번에 표시하려다 음표와 손가락 번호가 겹침
**해결**:
- 좁은 화면: 1개 마디만 표시하여 충분한 간격 확보
- 첫 마디: 음자리표와 박자표를 위한 추가 여백 (80px) 할당
- VexFlow Formatter에 적절한 너비 전달

### 3. 음표 하이라이트 매핑 수정
**문제**: 목표 '솔'인데 하이라이트가 '미' 위치에 표시됨 (인덱스 산술 오류)
**해결**:
- 각 StaveNote SVG 요소에 `data-note-index` 속성 부여
- 취약한 `.vf-notehead` 인덱스 계산 대신 명시적 속성 조회 사용
- `querySelector('[data-note-index="N"]')`로 정확한 매칭

### 4. iPhone 레이아웃 및 Safe Area 지원
**문제**: 
- Dynamic Island / 노치가 UI 가림
- 소프트 키보드가 거의 보이지 않음 (몇 픽셀만 노출)

**해결**:
- `viewport-fit=cover` 메타 태그 추가
- CSS `env(safe-area-inset-*)` 적용:
  - `body`: left/right 패딩
  - `.practice-header`: top 패딩
  - `.practice-footer`: bottom 패딩
- 가로 모드 키보드 축소: 높이 60px, 간격 최소화

### 5. 화면 회전 지원
**추가**: ResizeObserver로 컨테이너 크기 변화 감지
- 세로 ↔ 가로 회전 시 악보 자동 재렌더링
- 마디 개수 자동 조정 (1개 ↔ 2개)

## 테스트 단계

### 수동 테스트 (iPhone 실제 기기 또는 시뮬레이터)

1. **초기 설정**
   ```
   - Safari에서 http://localhost:5173 접속
   - 기기를 가로 모드로 회전
   - "학교 종이 땡땡땡" 곡 선택
   - "테스트 모드" 활성화 (마이크 권한 불필요)
   ```

2. **마디 슬라이딩 확인**
   ```
   - 첫 화면: 1번 마디만 표시 (솔-솔-라-라)
   - 소프트 키보드로 G-G-A-A 차례로 연주
   - 4번째 음표(라) 완료 후 → 2번 마디로 자동 전환
   - 2번 마디: 솔-솔-미(반음) 표시
   - 1번 마디가 사라지고 2번 마디만 보임 (겹침 없음)
   ```

3. **음표 겹침 검사**
   ```
   - 각 마디에서 음표 머리(notehead)가 명확히 분리되어 있는지 확인
   - 손가락 번호(1, 2, 3, 5)가 음표 위에 겹치지 않고 표시되는지 확인
   - 음자리표, 박자표가 첫 마디(전체 곡 기준)에만 나타나는지 확인
   ```

4. **하이라이트 매핑 정확도**
   ```
   - 화면 하단 "목표: 솔" 표시 시 → 악보의 '솔' 위치가 파란색으로 표시
   - 정답 연주 시 → 해당 음표가 초록색으로 변경
   - 오답 연주 시 → 해당 음표가 빨간색으로 깜빡임 (300ms)
   - 각 음표마다 목표 계이름과 하이라이트 위치가 일치하는지 확인
   ```

5. **Safe Area 및 UI 가시성**
   ```
   - 상단 헤더: Dynamic Island/노치에 가려지지 않음
   - 하단 키보드: 완전히 표시됨 (잘리지 않음)
   - 측면: 라운드 코너나 안전 영역 침범 없음
   - 버튼: 터치 가능한 크기 유지 (44×44pt 이상)
   ```

6. **회전 테스트**
   ```
   - 연습 중 세로 모드로 회전 → 악보 재렌더링 확인
   - 다시 가로 모드로 회전 → 현재 마디 유지 확인
   - 크기 변화 시 레이아웃 깨짐 없음
   ```

### Chrome DevTools 테스트 (데스크톱)

```bash
# 1. Chrome에서 http://localhost:5173 열기
# 2. DevTools 열기 (F12)
# 3. Device Toolbar 활성화 (Ctrl+Shift+M)
# 4. iPhone 14 Pro 선택 → 가로 모드 (Rotate 아이콘)
# 5. 위 수동 테스트 1-6 반복
# 6. 추가: iPhone SE (667×375) 에서도 동일하게 테스트
```

## 예상 결과

### ✅ 성공 기준
- [ ] 음표 9/24에서 2번 마디만 표시 (1번 마디 사라짐)
- [ ] 모든 마디에서 음표 겹침 없음
- [ ] 목표 계이름과 하이라이트 위치 일치
- [ ] 소프트 키보드 완전히 보임 (60px 높이)
- [ ] Dynamic Island 영역 침범 없음
- [ ] 회전 시 즉시 재렌더링

### ❌ 실패 징후
- 마디가 2개씩 계속 표시됨
- 음표나 손가락 번호 겹침
- 하이라이트가 잘못된 음표에 표시
- 키보드가 몇 픽셀만 보임
- 노치/아일랜드가 헤더 가림

## 빌드 확인

```bash
npm run build
# ✓ tsc 타입 체크 통과
# ✓ vite build 성공
# ✓ dist/ 폴더 생성
```

## 관련 파일
- `src/modules/ui/ScoreRenderer.ts` - 마디 윈도우 및 렌더링 로직
- `src/components/PracticeScreen.tsx` - ResizeObserver
- `src/styles/app.css` - Safe area, 가로 모드 스타일
- `index.html` - viewport-fit=cover
